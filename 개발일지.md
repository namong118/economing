# ECONOMING 개발일지

---

## 2026-07-30

가이드 페이지 스크린샷 화질 문제를 근본 원인부터 고치고, 프로필 페이지 톤을 다시 다듬은 날. 이번부터 커밋·푸시·배포는 매번 명시적으로 요청받을 때만 하기로 함.

### 가이드 페이지 스크린샷 12장 재캡처 — 화질 문제 원인 규명

- "화질이 안 좋아 보인다"는 지적을 받고, 먼저 코드는 건드리지 않고 원인만 확인. `sharp`로 PNG를 잘라 4배 확대해보니 텍스트 가장자리가 여러 픽셀에 걸쳐 뭉개져 있었음 — 12장 중 무작위로 뽑은 3장 모두 같은 패턴이라 개별 파일이 아니라 파이프라인 전체 문제로 판단
- 원인: 기존 캡처 방식(iframe + 마젠타 배경 트림 + 860×1500 리사이즈)에서, 트림 후 실제 크기가 목표 치수와 정확히 안 맞아 매번 미세하게 리샘플링되며 화질이 깎였음
- 해결: `chrome-devtools` MCP의 `emulate` 도구가 CDP `Emulation.setDeviceMetricsOverride`를 그대로 노출한다는 걸 확인 — `viewport: "430x750x2,mobile,touch"` 한 줄로 뷰포트·DPR·모바일 모드가 정확히 걸리고 `window.innerWidth === 430`이 실제로 성립함(브라우저 확장의 `resize_window`와 달리 신뢰 가능). 이러면 iframe도 트림도 리사이즈도 필요 없이 캡처 결과가 처음부터 860×1500 무손실로 나옴
- 12장 전부 이 방식으로 재캡처, 픽셀 치수 동일 확인 + 확대 비교로 화질 개선 확인. `docs/guide-screenshots.md`에 새 방법을 표준으로 기록

### 가이드 페이지 — 경제사전 항목 신설

- "낯선 용어를 만나면 — 사전에 저장합니다" 항목이 스크린샷 없이 텍스트만 있던 상태였음(코드 주석에 "이미지 없는 항목 — 스크린샷 대상에서 제외됨"이라고 표시돼 있었음)
- 실제로 노밍 코치에게 질문해 답변을 받고, 핵심 용어의 "사전 저장" 버튼을 눌러 진짜로 2개를 저장한 뒤 내 성장 탭의 경제사전 화면을 캡처(`13-dictionary.png`) — 다른 항목과 동일한 좌우 배치 카드로 추가하고 `docs/guide-screenshots.md` 표에도 반영

### 프로필 소개 페이지 — 톤이 어긋나는 섹션 정리

- "이번에 처음 해본 것" 섹션이 Tech 페이지의 기술 스택 목록과 거의 동일한 문구를 그대로 나열하고 있어서, 앞선 섹션들(경력·전환·이전 경험이 이 프로젝트에 어떻게 쓰였는지)의 개인 회고 톤과 부딪힌다는 지적을 받음
- 논의 후 섹션을 통째로 삭제하기로 결정. 마무리 문구("전부 처음이었습니다")는 처음엔 앞 섹션 끝으로 옮겼다가, 최종적으로는 그것도 삭제 — "시각정보디자인 → 혼자 만들어도 정돈된 화면" 카드로 페이지가 끝나는 구성이 됨

### 작업 방식 관련 피드백

- 이전엔 "빌드→커밋→배포"를 한 세트로 반복 요청받아 그대로 수행해왔는데, "앞으론 내가 허락하기전까지 커밋 배포 푸시 하지마"라는 명시적 지시를 받음. 반복된 패턴이 상시 허가는 아니라는 것 — 이제부터는 빌드까지만 하고 커밋·푸시는 매번 명시적 요청을 받을 때만 진행

### 참고 — Tech 페이지 통계는 자동 갱신되지 않음

`STATS`/`COMMIT_TYPES`(커밋 수, feat/fix 비율, 코드 줄수 등)는 `TechStackPage.jsx`에 하드코딩된 값이라 실시간으로 계산되지 않는다. 커밋이 쌓일수록 다시 낡아지므로, 필요할 때마다 수동으로 재계산해 갱신해야 함(7/29에 한 번 갱신했고, 오늘 커밋 3개가 더해져 이미 다시 낡은 상태).

---

## 2026-07-29

리부트 AI 활용대회 제출을 앞두고, 발표용 페이지를 다듬고 앱 곳곳에 남아있던 개발자 전용 흔적을 정리한 날.

### 프로필 소개 페이지 — 히어로 레이아웃

- 이름(`박남영`)과 소개문을 `alignItems: 'baseline'`으로 한 줄에 나란히 배치했었는데, 이름 폰트가 52px로 커서 소개문이 이름 아래쪽에 눌린 것처럼 보이던 문제 — 세로 배치로 변경
- 소개문을 문장 하나로 이어 자동 줄바꿈에 맡겼더니 "지금은 AI" / "개발자가 되기 위해..."로 갈라지는 문제 — 문장 단위 `\n`으로 줄바꿈 지점을 직접 지정
- 페이지 하단에 텍스트로만 있던 이메일·GitHub 링크를 이름 옆 아이콘으로 옮기는 과정에서 `lucide-react`(v1.17) 최신 버전에 GitHub 로고 아이콘이 없다는 걸 발견(브랜드 로고라 라이브러리에서 제외됨) — 표준 옥토캣 SVG를 직접 인라인 컴포넌트로 추가해서 해결
- 아이콘만 있으면 주소를 알 수 없다는 피드백에 따라, 이메일·GitHub 모두 "아이콘 + 실제 주소" pill 형태로 최종 확정. 폰트도 처음엔 모노스페이스로 했다가 사이트 전반과 안 어울린다는 피드백에 기본 폰트로 통일

### 발표용 페이지 — 전문용어 순화, 줄바꿈 버그 두 종류

- Tech 페이지 트러블슈팅 항목에 `check-content.mjs`, `recordBiteView`, `OIDC id_token` 같은 내부 코드 식별자가 설명 없이 그대로 노출돼 있어서, 발표 자료를 보는 일반 독자가 이해하기 어렵다는 지적 — 코드명은 이미 앞 섹션("품질 관리")에서 소개한 "정합성 검사 스크립트"라는 말로 통일해서 가리키고, `adb logcat`/`Custom Tabs`/`Credential Manager`처럼 실제 존재하는 기술 용어는 남기되 괄호로 짧은 우리말 설명을 병기. 이 과정에서 향후 계획 표의 콘텐츠 후보 개수가 문서 표(224개)와 어긋난 채 190개로 남아있던 것도 발견해 같이 정정
- 히어로 제목("AI가 진단하고, 사람이 검증하고, AI가 돕습니다")이 화면 폭에 딱 맞아떨어지면서 "돕습니다"가 "돕습니/다"로 음절 중간에서 잘리는 버그 발견 — 한글은 기본적으로 음절 단위 줄바꿈이 허용되기 때문. `word-break: keep-all`을 추가해 해결하고, 같은 구조(vw 기반 큰 히어로 폰트)를 쓰는 About·Guide 히어로에도 선제적으로 같은 처리 적용
- 기존에 "영문 토큰이 한글과 분리되는 경우"만 잡던 줄바꿈 검사 스크립트가 이 순수 한글 단어 내부 절단은 못 잡는다는 걸 확인하고, 단어 하나가 두 줄에 걸쳐 렌더링되는지 직접 감지하는 스캐너를 새로 만들어 4개 발표 페이지 전체를 재검증
- About 페이지 "오늘의 경제를 읽습니다" 섹션 중 지표 유형 설명이 Body 문단 13개가 이어지는 "글로만 된 벽"이었던 부분을, 앞서 다른 섹션에서 쓰던 `ProblemCard`/`CompareCard`를 재사용해 카드형으로 재구성(지표 유형 3카드 + 기준선 설정 2단 비교). `CompareCard`는 라벨을 바꿀 수 있게 `beforeLabel`/`afterLabel` prop만 추가하고 기존 용도는 그대로 유지

### 랜딩페이지 — QR 실제 이미지, 스크롤 리빌 애니메이션

- `ComingSoonSection`에 "곧 제공 예정"이라며 자리만 잡아두었던 QR 플레이스홀더를, `BrandPanel`에서 이미 쓰던 `qr-mobile.png`로 교체해 실제로 작동하는 QR 코드를 노출
- `IntersectionObserver` 기반 `Reveal` 래퍼 컴포넌트를 새로 만들어 랜딩페이지 8개 섹션 전체에 적용 — 스크롤로 뷰포트에 들어오면 부드럽게 fade+slide up. 이미 `BrandPanel`에 있던 `.bp-reveal`(마운트 시 스태거 등장)과는 트리거 방식이 달라서(스크롤 진입 vs 마운트) 별도 클래스(`.reveal`)로 분리했지만, 이징·톤은 동일하게 맞춤

### 제출 전 개발자 전용 코드 정리

- "내 성장" 탭에 프로덕션에도 그대로 노출돼 있던 개발자 메뉴(학습 진도 초기화 버튼) 제거
- 개발자 메뉴 제거로 호출부가 사라진 `resetBiteProgress`(biteService), 안 쓰이는 구 진단문항 배열 `_REMOVED`(OnboardingPage, 113줄), 앱 어디서도 import되지 않는 죽은 파일 `dummyData.js`를 찾아서 함께 삭제
- `VITE_MOCK_AUTH` mock 모드, `console.log`, 레거시 AI 추천 함수(`getRecommendedBiteAI`, 주석에 "지우지 말 것" 명시) 등도 훑어봤지만 — mock 모드는 `.env.local`과 배포 워크플로 양쪽에서 `false`로 고정돼 실제 배포에 영향 없고, `console.log`는 프로젝트 전체에 한 건도 없고, 레거시 함수는 의도적으로 남겨둔 것이라 셋 다 그대로 둠

### 빌드 확인 & 커밋

- `npm run build` 성공, 에러 없음(청크 크기 500KB 초과 경고만 있음 — 기존부터 있던 경고로 이번 작업과 무관)
- 파일 단위로 겹치지 않게 5개 커밋으로 분리: 프로필 히어로 → 테크 용어/줄바꿈 → 어바웃/가이드 줄바꿈·카드화 → 랜딩 QR·리빌 애니메이션 → 개발자 전용 코드 정리

---

## 2026-07-28

인포그래픽 컴포넌트의 오래된 제약 하나를 풀고, 발표용 페이지에 트러블슈팅·향후 계획을 채운 날.

### CompositionInfographic — 덧셈만 되던 문제 해결

- `CompositionInfographic`이 항목 사이 구분자로 `+`를 하드코딩하고 있어서 "자산 − 부채 = 순자산"처럼 뺄셈으로 표현해야 하는 개념을 만들 수 없었던 문제(7/26 개발일지에 남겨둔 과제) 해결
- `op` prop 추가(`add`/`subtract`/`multiply`/`divide` → `+`/`−`/`×`/`÷` 매핑). 기본값은 `add`라 기존 15개 호출부는 수정 없이 그대로 동작
- id 83 "순자산 계산 흐름"을 실제 테스트 케이스로 사용 — 당시 `+`가 고정이라 `flow`형으로 우회했던 카드를 이번에 `CompositionInfographic op="subtract"`로 되돌림
- 작업 지시서에는 Solar AI 동적 생성 스키마(`infographicService.js`)에도 `op` 필드를 추가하라는 항목이 있었지만, 확인해보니 `CompositionInfographic`은 정적 데이터(`biteInfographics.jsx`)에서만 직접 호출되고 AI 동적 생성 경로(`InfographicCard.jsx` → `flow`/`collection`/`compare`)와는 아예 연결돼 있지 않았음 — 아무도 소비하지 않는 필드를 추가할 뻔한 걸 미리 확인해서 걸러내고, 이 부분은 범위에서 제외

### tech 페이지 — 트러블슈팅 · 향후 계획 섹션 추가

- 발표용 `/tech` 페이지의 "품질 관리"~"커밋 이력" 사이, 그리고 맨 마지막에 두 섹션 신설
- 트러블슈팅: 안드로이드 실기기 로그인 버그, AI 검증 질문 방식에 따른 76%→13% 반전, 퀴즈 정답 위치 편향, 미연결 코드(`recordBiteView`) — 이미 개발일지에 남아 있던 네 가지 사례를 페이지 톤에 맞춰 재구성
- 향후 계획: 커리큘럼 확장, 복습 설계, 지표 실측 전환, 카카오 네이티브 로그인, 스토어 배포 5가지를 `DataTable` + `ChecklistItem` 조합으로 정리
- 기존 페이지에 정의된 컴포넌트(`SectionTitle`/`Body`/`Quote`/`StepHeading`/`ChecklistItem`/`DataTable`/`Mono`)만 재사용, 새 컴포넌트는 만들지 않음

### 커밋 & 배포

- 커밋: `fix: CompositionInfographic 연산 기호 하드코딩 수정 — op prop 추가`, `feat: tech 페이지에 트러블슈팅·향후 계획 섹션 추가`, `docs: 2026-07-28 개발일지 정리`
- GitHub main 브랜치 푸시, 웹(GitHub Pages) 배포

---

## 2026-07-27

발표 자료용 페이지를 만든 날. 코드보다 **말로 정확히 설명하는 것**이 더 오래 걸렸다.

### 배경 리디자인 — "패널을 넓히지 않고 여백을 없앤다"

- 데스크톱 화면에서 브랜드 패널(640px) 바깥 여백이 배경 없이 흰 화면으로 비어 보이는 문제
- 첫 시도(`.ds-brand`를 `flex:1`로 늘려 여백 자체를 없앰)는 되돌림 — 패널 폭·레이아웃은 원래 값이 맞고, 문제는 "배경이 패널 크기에 갇혀 있다"는 것이었기 때문. 증상과 원인을 분리하지 못하면 멀쩡한 값을 건드리게 된다는 걸 다시 확인
- 해결은 배경을 `.ds-brand`가 아니라 `.ds-root`(화면 전체)에 얹는 것. 블롭도 패널 전용(`.bp-blob`)에서 루트 레벨(`.ds-blob`)로 옮겨 앱 컬럼 뒤로 자연스럽게 지나가게 함
- 마우스를 따라다니는 스포트라이트 효과는 만들어서 붙여봤지만 결국 제거. 붙이는 과정에서 원인이 두 겹인 버그를 만났음:
  1. `onMouseMove`가 패널 DOM에 직접 바인딩돼 있어 커서가 패널 경계를 벗어나는 순간 딱 끊김
  2. 스포트라이트 span 자체가 패널과 같은 크기(`inset:0`)라, `overflow`와 무관하게 그 span 자기 자신의 박스 밖으로는 배경이 그려질 수 없음
  둘 다 고쳐서 정상 동작까지 확인했지만, 효과 자체가 있으나 마나 해서 그냥 뺐다 — 고친 방법(이벤트 스코프와 배경-박스-크기는 별개 문제)은 기록해둘 가치가 있어서 남김

### 발표용 페이지 4개 — 앱과 다른 레이아웃을 신설

- `/about /profile-intro /guide /tech` 전용 `PresentationShell` 신설. 좌측 브랜드 패널 고정, 우측만 내부 스크롤 — 기존 `DesktopShell`과 구조는 같지만 완전히 별개 레이아웃
- 발표용 페이지는 데스크톱 전용으로 정함 — 모바일에서 발표 페이지를 별도로 대응하는 대신, `PresentationShell`이 1024px 미만이면 `/home`으로 리다이렉트. 리사이즈로 폭이 좁아지는 경우도 `matchMedia`의 `change` 이벤트로 동일하게 처리해서, 페이지 콘텐츠 쪽에서는 모바일 대응을 아예 안 해도 되게 만듦 (About/Tech 둘 다 가로 스크롤·표 축소 같은 모바일 대응 코드가 없음)
- About·Tech 두 페이지는 원고를 그대로 쓰는 게 원칙이라 문구를 임의로 바꾸지 않았음. About은 작성 후 아키텍처 섹션 삭제 요청이 왔는데, SVG 컴포넌트 자체는 남겨서 Tech 페이지에서 그대로 재사용 — 페이지 하나 지웠다고 안에 있던 재사용 가능한 조각까지 같이 버릴 필요는 없었음
- Tech 페이지는 작업 중간에 원고가 한 번 바뀌었음(v1→v2) — 히어로 헤드라인·통계 7종 구성, "Solar AI 특성 3가지"가 대응 5단계 앞에 추가됨, 문서 표가 3열→2열로 축소. "원고 그대로 쓴다"는 원칙 때문에, 이미 만든 페이지라도 새 원고가 오면 전체를 다시 대조해서 문구 단위로 맞췄음
- 개발일지 발췌(Google 로그인 버그 기록)를 원문 그대로 넣어야 했는데, 접기/펼치기에 JS를 쓰면 "JS 시각 효과 추가 금지" 제약과 충돌함 — 네이티브 `<details>/<summary>`를 쓰면 JS 없이 접기/펼치기가 되므로 제약을 위반할 수 없는 방식으로 해결

### 검증 방식

- 브라우저 확장의 `resize_window`가 요청한 폭을 신뢰할 수 없게 반환하는 문제(같은 요청에도 매번 다른 값)가 있어, 로그인이 필요 없는 페이지는 새 탭에 `iframe`을 정확한 픽셀 크기로 주입해 검증했음 — 1280px/386px처럼 정확한 폭이 필요한 모바일 리다이렉트 테스트에 특히 유효
- 코드가 아니라 시각적 결과를 검증해야 하는 작업이라, 매 섹션마다 스크린샷으로 직접 확인하고 넘어감

### 커밋

작업을 4개로 나눠 커밋: 배경 리디자인 → 발표 페이지 인프라 → About → Tech. 배경 리디자인 커밋에는 발표 페이지 메뉴가 섞여 들어가지 않도록, 같은 파일(`BrandPanel.jsx`, `index.css`) 안에서 두 작업이 겹친 부분을 손으로 분리해서 순서대로 커밋함.

---

## 2026-07-26

오늘은 기능을 만든 날이 아니라 **콘텐츠 확장 방향을 결정한 날**. 뉴스 기반 콘텐츠 자동 생성이 실제로 말이 되는지 측정해봤고, 그 과정에서 판단이 여러 번 뒤집혔다.

### 판단이 뒤집힌 4가지

#### relatedTerms 확장 시도 → 기여 0%

- 뉴스 키워드 매칭률을 높이려고 `relatedTerms`를 확장 어휘로 쓰려 했으나, 실측해보니 60개 중 55개가 이미 다른 한잎의 제목이었음 — 필드 이름만 보고 "새 어휘가 있겠지"라고 판단한 게 원인
- 실측하지 않았다면 잘못된 전제 위에 매칭 로직을 설계할 뻔함

#### Tier 3 지표의 함정

- "카테고리 존재 여부" 기준 커버리지가 71.6% → 85.3% → 95.9%로 계속 올랐지만, 그 기간 실제 콘텐츠는 70개 그대로였음
- 결정적 증거: 매핑 테이블에 `'미국경제'` 한 줄만 추가하면 남은 갭이 0개가 되는 구조였음 — 콘텐츠를 하나도 안 만들어도 갭이 사라지는 지표는 잘못된 지표라고 보고 추가하지 않음
- 측정 지표를 잘못 고르면 아무것도 안 하고도 개선된 것처럼 보인다는 걸 직접 확인한 사례

#### AI 검증은 질문 방식이 결과를 만든다

- "이 매핑이 맞나?"로 물었을 때: ALIAS 76%
- "틀렸다고 가정하고 틀린 이유를 찾아라" + 한잎 본문(요약/설명/왜 중요한지) 첨부로 다시 물었을 때: **13%**
- 같은 모델, 같은 데이터. 첫 번째 결과를 그대로 믿었다면 거의 효과 없는 작업을 "해결됐다"고 판단했을 것
- 정답 후보 81개 제목을 프롬프트에 전부 넣어줬는데도 7건은 존재하지 않는 한잎을 그럴듯하게 지어냄 → **참조 필드는 사람이 보기 전에 코드로 먼저 대조해야 한다**는 교훈, 이게 이후 `check-content.mjs`로 이어짐

#### 퀴즈 정답 위치 편향

- 기존 60문항을 확인해보니 1번 자리 정답 0개, 4번 1개 — 98%가 2~3번에 몰려있어 사실상 2지선다
- 콘텐츠를 늘리는 과정에서 우연히 발견. 이번에 새로 쓴 10문항도 검토 없이 그냥 썼다면 9개가 2번이었음
- 사람이든 AI든 "명백한 오답을 먼저 쓰고 정답을 마지막에 배치"하는 습관이 원인으로 추정
- 고정 시드 셔플로 18/18/17/17 균등화, 정답 **텍스트 문자열** 대조로 검증 (인덱스 대조가 아님 — 셔플 버그가 나도 인덱스만 보면 항상 맞아 보이기 때문)

### 측정 결과

- 뉴스 420건에서 경제 개념어 독립 추출 → 기존 콘텐츠와 대조
- 기사 단위 커버리지 **21.6%** (사람 검토를 통과한 ALIAS 5건 반영 기준)
- 히트 상위 10개가 전체의 **96.8%** 차지 — 소수 콘텐츠만 반복 노출되는 구조
- 한계 효용 곡선: 신규 콘텐츠 5개 지점에서 커버리지가 48.0%로 급등, 그 이후 완만해짐 (0→5구간 콘텐츠 1개당 4.7~7.0%p 상승, 5→10구간은 1.06%p로 급락)

### 결정

- AI 콘텐츠 자동 생성 파이프라인 **보류** — 검증 비용이 제작 비용을 초과할 가능성이 확인됨
- 뉴스 연동 무한 생성 대신 **유한한 커리큘럼**(초보자 필수 경제 용어 100개 → 이후 200개) 방향으로 선회. 끝이 있는 구조라 진도·완주 자체가 리텐션 장치로 작동할 수 있음
- 모든 단어에 인포그래픽·퀴즈를 붙이는 건 가능하다고 판단 — `BiteInfographic`이 템플릿 기반(3~4단계 데이터 10여 줄)이고 아이콘이 38종의 닫힌 목록이라 실수하면 빌드 단계에서 잡힘
- 갭 개념 224개를 `docs/content-backlog.md`로 이관해 콘텐츠 제작 목록의 원천으로 보존

### 정합성 검사 도입과 발견

- 실생활경제(id 71~80) 퀴즈 10개가 통째로 누락된 걸 발견 — 에러 없이 조용히 실패하는 유형이라 오래 방치됐고, 홈의 "한잎 퀴즈 풀기" 할일이 **7일에 하루꼴**로 완료 불가 상태였음
- 수동 확인의 한계를 인정하고 `scripts/check-content.mjs` 도입 (한잎/퀴즈/인포그래픽 짝 누락, 고아 id, id 중복, relatedTerms 깨진 참조, 퀴즈 구조, 정답 위치 분포 검사)
- 실행해보니 예상 못한 문제도 걸림:
  - `relatedTerms` 6건이 존재하지 않는 한잎을 참조 (id 51 `'기초'`는 카테고리명을 잘못 넣은 오기, 손익계산서·탄력성·독점·순자산은 아직 안 만든 실제 개념) — 에러 없이 설명 줄이 비고, 나만의 사전에 뜻이 빈 문자열로 저장되는 문제로 이어짐
  - 지표 id 81(코스닥) 인포그래픽 누락
  - 전부 해소, `exit code 0` 확인
- 스크립트를 만들 때 **"예상 결과를 미리 적어두는" 방식**이 유효했음 — 검사는 통과가 기본값이라 망가져도 티가 안 나서, 아무것도 안 걸리면 오히려 파싱이 실패했다는 고장 신호로 봐야 함

### 남은 과제

- 히트 편중 96.8% — 콘텐츠를 늘려도 저절로 해소되지 않음. 최근 노출 이력 기반 감점 로직이 별도로 필요
- `BiteInfographic` 레이아웃이 3단계 흐름도 1종뿐 — 200개로 확장하면 단조로워지므로 콘텐츠 생산 전에 변형 2~3종 추가 필요
- 퀴즈 `explanation` 필드는 71~80에만 작성됨. 1~60은 미작성 상태로 남음

### 다음 작업

1. 인포그래픽 레이아웃 변형 2~3종 추가 (콘텐츠 생산 전)
2. 갭 목록 224개에서 초보자용 단어 100개 선별
3. 30개 작성해 100개 완성 — 이때 개당 실제 소요 시간을 측정하고, 그 값으로 200개 확장 여부 및 AI 초안 도입 여부를 판단

### 커밋 & 배포

- 커밋: `feat: 뉴스 키워드 기반 콘텐츠 갭 분석 스크립트 추가`, `fix: 퀴즈 정답 위치 편향 수정 및 실생활경제 퀴즈 10개 추가`, `chore: 일회성 분석 스크립트 정리 및 supabase temp 추적 해제`, `feat: 콘텐츠 정합성 검사 스크립트 추가`, `fix: relatedTerms 깨진 참조 정리 및 코스닥 인포그래픽 추가`
- GitHub main 브랜치 푸시

### 오후

오전에 결정한 유한한 커리큘럼을 실제로 만든 시간.

**한 일**

- 한잎 70개를 11챕터 96개 초보자 커리큘럼으로 재편
- 신규 카드 31장 작성(본문·퀴즈·인포그래픽 3종 완비), 96/96 완성
- 인포그래픽 레이아웃 2종 추가(구성형·비교형), 기존 15개 전환
- 아카이브를 챕터 구조로 재구성, 오늘의 한잎을 AI 추천에서 커리큘럼 순서 기반으로 교체

**판단이 뒤집힌 것**

**선행 개념 그래프 실패** — "A를 알아야 B를 안다"로 순서를 잡으려 했으나 70개 중 44개가 선행 없음, 고립 노드 36개. 커리큘럼의 줄기로 쓰기엔 너무 성겼음. 대신 얻은 것: hard 13개 중 10개가 선행 개념 0개 — 난이도와 선행 깊이는 별개 축이라는 확인.

**difficulty 태그가 못 쓸 필드였음** — 인플레이션=easy인데 디플레이션=medium, ETF=easy인데 인덱스 펀드=medium, 환율=medium인데 그 응용 시나리오는 easy. 콘텐츠를 쓰며 그때그때 붙인 배지용 태그라 정렬 축으로 부적합. 화면에서 제거.

**v1의 고정 10×10 틀** — 정원을 채우려고 빈도 0~1짜리 20개를 넣고, 정작 빈도 1~3위를 맨 뒤에 둠. 목표("먼저 알아야 뉴스가 읽힌다")와 정반대 결과. 틀을 풀자 96개 11챕터로 자연스럽게 갈림.

**집계 방식이 결론을 바꿈** — 순서 위반 34건을 한 줄씩 보면 전부 사소해 보였는데, 출처별로 묶으니 한 챕터에서 21건이 한 방향으로 몰려 있었음. 같은 데이터, 다른 집계.

**지표 포화를 두 번 겪음** — 오전 Tier 3는 콘텐츠 증가 없이 71.6%→95.9%로 상승(위로 포화). 오후 순서 위반 검사는 챕터를 어떻게 재배치해도 38건에서 안 움직임(아래로 포화). 둘 다 "더 이상 정보가 없는 지표"라 판단하고 사용 중단.

**AI 지시를 그대로 따랐으면 틀린 화면이 나올 뻔한 건** — 순자산 인포그래픽에 구성형을 쓰라고 했으나, 컴포넌트를 열어보니 "+"가 하드코딩돼 있어 "자산 + 부채 = 순자산"이라는 틀린 수식이 표시될 상황. 흐름형으로 대체.

**남은 과제**

- CompositionInfographic이 덧셈만 지원 — op를 prop으로 받게 개선 필요
- check-content.mjs의 기대값이 하드코딩 — 파일에서 자동 계산하도록
- 학습 진도 기능은 표시만 있고 실제 완료 처리 로직은 미구현
- 96개 다음 단계는 docs/content-backlog.md의 남은 190개

---

## 2026-07-25

### 작업 내용

#### Capacitor 안드로이드 앱 신규 구축

- 웹(GitHub Pages)으로만 서비스하던 ECONOMING을 실제 안드로이드 앱으로 패키징
- `@capacitor/core`, `@capacitor/android`, `@capacitor/cli` 설치, appId `com.economing.app`
- `vite.config.js`에 `capacitor` 모드 분기 추가 (GitHub Pages용 base path `/economing/` vs 앱 번들용 `/`)
- `npm run build:capacitor`, `npm run cap:sync` 스크립트 추가
- 실제 갤럭시 S24(USB 디버깅)에 설치해서 매 단계 직접 검증 (adb install/logcat/screencap 활용)

#### Google/카카오 로그인 버그 발견 및 해결

- 실기기 테스트 중 발견: 소셜 로그인 시 앱이 완전히 웹사이트로 이탈해서 못 돌아오는 문제
  - 원인: `signInWithOAuth`가 앱의 WebView를 직접 이동시키는 방식이라, 네이티브 앱 안에서는 `redirectTo`가 실제 도메인이 아니라서 로그인 후 앱으로 복귀 불가
- 1차 수정: `@capacitor/browser` + `@capacitor/app`으로 인앱 브라우저(Custom Tabs) + 커스텀 URL 스킴(`com.economing.app://login-callback`) 딥링크 방식으로 전환, Supabase Redirect URLs에도 등록
- 그런데 실기기(삼성 갤럭시)에서 재현 테스트 결과, **삼성 인터넷의 Custom Tabs 구현이 커스텀 스킴 딥링크를 앱으로 못 넘겨줌** — `adb logcat`/`dumpsys window`로 실제 포그라운드가 `com.sec.android.app.sbrowser`임을 직접 증명
- 최종 해결: Google 로그인을 `@capgo/capacitor-social-login`(Credential Manager 기반 네이티브 로그인)으로 전면 교체 — 브라우저를 아예 거치지 않아 이 문제 자체가 발생하지 않음. Supabase `signInWithIdToken`으로 세션 발급
  - Google Cloud Console에 Android용 OAuth 클라이언트 추가 등록 필요 (패키지명 + 디버그 키스토어 SHA-1)
- 카카오는 이번엔 네이티브 SDK로 못 넘어감 — 조사해본 커뮤니티 플러그인(`karbon-capacitor-kakao-plugin`)이 Supabase가 요구하는 OIDC id_token을 아예 반환하지 않는 걸 소스 코드까지 열어서 확인 → 카카오는 기존 인앱 브라우저 방식 유지, 네이티브 전환은 추후 과제로 남김

#### Android 15+/16 edge-to-edge 강제 적용으로 인한 화면 잘림 수정

- targetSdkVersion 36 기준으로 Android가 강제하는 edge-to-edge 때문에 앱 콘텐츠 상/하단이 상태바·제스처바에 가려 잘려 보이는 문제 발견
- `@capawesome/capacitor-android-edge-to-edge-support` 플러그인으로 해결 — 기존처럼 시스템 바 영역을 피해서 콘텐츠가 그려지도록 복원, 상태바/네비게이션바 색상도 앱 배경색(`#F4FAF6`)에 맞춤

### 검증

- 실제 갤럭시 S24에서 앱 설치 → Google 네이티브 로그인 → 홈 화면까지 전 과정 실기기로 확인
- `adb logcat`으로 로그인 성공/실패 원인을 매번 실제 로그 기반으로 진단 (추측 대신 증거로 디버깅)

### 커밋 & 배포

- 커밋: `feat: Capacitor 안드로이드 앱 추가 + 네이티브 구글 로그인 전환`
- GitHub main 브랜치 푸시
- 웹(GitHub Pages) 배포

---

## 2026-07-18

### 작업 내용

#### 지표 읽는법 기능 신규 구축 (`/indicator/:id`)

- **배경**: 경제읽기 페이지에 코스피/코스닥/환율 실시간 티커를 추가했는데, 처음엔 탭하면 경제사전(TermsPage)으로 보냈음. "그래프를 보면서 읽는법을 배우게 하고 싶다"는 요청으로 전용 페이지를 새로 만듦.
- 지표읽기 콘텐츠(구 `economicBites` id 61~70, 코스피/환율/CPI/기준금리/무역수지/실업률/GDP/PER/장단기금리역전/공포탐욕지수)를 경제한잎에서 분리 → `src/data/indicatorsData.js`
- 코스닥(id 81) 신규 항목 추가 (기존엔 없었음)
- `src/components/indicators/StaticTrendChart.jsx`: 인터랙션 없는 정적 SVG 차트. 오늘/최고/최저 라벨을 처음부터 표시. 실제 데이터는 실선(상승 빨강/하락 파랑, 한국 관례), 예시 데이터는 회색 점선 + "예시 그래프" 배지로 구분
- `IndicatorPage.jsx`: 히어로(다크그린) + 인포그래픽(기존 `BITE_INFOGRAPHICS` 재사용) + 그래프 + 4단 설명(쉽게 설명 → 왜 중요한지 → 실제 사례 → 내 삶에 미치는 영향) + 다른 지표 보기 + CTA(→ `/read`)
- `EconomicBiteArchivePage.jsx`: 카테고리 필터에서 "지표읽기" 제거 (80개 → 70개)

#### 실시간/실제 데이터 소스 연동

- `supabase/functions/indices/index.ts`: Yahoo Finance(`range=1mo&interval=1d`)로 코스피/코스닥 현재값+1개월 history, open.er-api.com으로 환율 현재값 + Frankfurter.app(무료, 키 불필요)으로 환율 history
- `supabase/functions/economic-stats/index.ts` (신규): 기준금리·CPI·실업률·GDP 성장률·무역수지·장단기금리역전(국고채 10년-3년 스프레드) — 한국은행 ECOS + 통계청 KOSIS API. 시행착오 끝에 6개 전부 실제 데이터로 성공:
  - KOSIS는 `/openapi/Param/statisticsParameterData.do` (경로에 `/Param/` 빠뜨려서 한동안 404)
  - 실업률은 같은 테이블에서 항목명 필터링 없이는 "실업자 수"(인원)를 잘못 집어옴 → 항목명 키워드 매칭으로 수정
  - 국고채 금리(817Y002)는 일별(D) 전용 — 월별로 요청해서 처음엔 실패
  - GDP는 KOSIS 100대지표 페이지에서 실제 tblId(`DT_200Y102`) 확인 후 성공
  - PER, 공포탐욕지수는 실시간 소스 없어 예시 그래프 유지
  - 월/분기 단위 통계라 24시간 인메모리 캐시 적용 (Edge Function cold start 시 초기화되는 한계 있음, 이 정도 트래픽엔 충분하다고 판단)
- 브라우저로 직접 확인하다가 버그 2개 발견·수정:
  1. `indices` 함수의 history 추가 코드를 작성만 하고 배포를 안 해서 코스피 차트가 빈 카드로 나오던 문제 → 재배포로 해결
  2. 최고/최저 라벨이 그래프 맨 앞/뒤 지점일 때 SVG 밖으로 잘리던 문제 → 위치에 따라 좌/우/중앙 정렬 자동 전환

#### 죽은 코드 정리 — TermsPage/DictionaryPage 삭제

- `TermsPage.jsx`(경제사전 검색), `DictionaryPage.jsx`(`/dictionary`), `vocabularyService.js`(`vocabulary` 테이블), `termData.js` 전부 삭제
- 확인 결과 이 4개는 메인 네비게이션 어디에도 연결 안 된 고립된 시스템이었음 (2026-06-10 최초 MVP 스캐폴딩 때 만들어진, 서비스 방향 전환 이전 컨셉의 잔재로 추정)
- "나만의 경제 사전" 기능 자체는 이미 `MyGrowthHubPage.jsx`의 "경제사전" 탭(`user_dictionary` 테이블, `SaveTermButton`으로 저장)이 대체하고 있어서 기능 손실 없음
- 삭제 전 `vocabulary` 테이블 실제 저장 데이터 0건 확인 (`supabase db query --linked`)

### 검증
- `npm run build`, `eslint` 통과
- Claude in Chrome으로 직접 브라우저 구동해서 확인 (티커 → 지표별 그래프 → `/bites` 카테고리/개수 → `/terms` 접근 불가까지)

#### 연속 학습일(스트릭) 시스템 연결

- `profileService.js`의 `updateStreak(userId)`는 이미 완전히 구현되어 있었지만 (연속일 계산 로직 정확함, mock/실제 둘 다 지원) 앱 어디서도 호출되지 않던 죽은 함수였음
- `HomePage.jsx`의 기존 "오늘의 할일" 완료 체크(한잎 읽기/퀴즈/노밍 대화/일기, 4개 중 하나라도 완료)에 연결
- 3/7/30/100일 마일스톤 도달 시 보너스 XP(+10/+30/+100/+300) 지급 + 상단 축하 토스트("🔥 N일 연속 학습! +XX XP")
- `MyGrowthHubPage.jsx` 요약 탭에 "N일 연속 학습 중" 카드 추가 (0일이면 숨김)
- Claude in Chrome + `supabase db query --linked`로 실제 계정 조작해서 3일 마일스톤 발동까지 실제 확인 (streak 2→3, XP 40→50, 재확인 시 중복 지급 안 됨)
- 마일스톤/뱃지 설계는 미리 논의: 뱃지는 영구 기록용 스키마·UI가 추가로 필요해서 이번엔 보류, XP 보너스로 기존 성장 시스템에 흡수시키는 방식으로 결정

#### 저장 실패를 조용히 무시하던 곳 8곳 추가 정리

스트릭 기능 테스트 중 `user_quiz_results` 테이블이 전체 0건인 걸 발견 — 퀴즈 정답 처리 로직의 `insert`가 에러 체크 없이 계속 조용히 실패하고 있었음. 이 참에 비슷한 패턴 전체를 훑어서 8곳 정리:

1. `EconomicBitePage.jsx` — 퀴즈 정답 시 XP 지급/`user_quiz_results` 저장 (버그 원인)
2. `onboardingService.js` — `completeOnboarding`의 로드맵/노밍인트로 저장
3. `RoadmapPage.jsx` — 단계 완료 처리 (롤백 + 알림)
4. `DictionaryContext.jsx` — 단어 삭제 `remove()` (롤백 + 알림)
5. `ResultPage.jsx` — 레벨/로드맵 초기화
6. `HomePage.jsx` — 스트릭 보너스 XP (오늘 만든 코드)
7. `readingService.js` — `markAsRead`: `addXp`가 에러를 던지지 않는데 try/catch로 잡으려 했던 구조적 문제 → 실제 에러 체크로 교체
8. `ReadingPage.jsx` — 읽기 완료 처리 (롤백 + 알림)

손 안 댄 것(낮은 우선순위로 기록만): `biteService.js recordBiteView`(의도된 설계, 주석 있음), `readingService.js saveCachedNews`(캐시 저장이라 저위험), `dictionaryService.js saveKeywordsFromNews`(뉴스 키워드 여러 개 반복 저장, 개별 실패 추적 안 됨 — 다음에 볼 것).

### 커밋 & 배포
- 커밋 1: `feat: 지표(코스피/코스닥/환율/기준금리 등) 읽는법 기능 추가 + 죽은 경제사전 화면 정리`
- 커밋 2: `feat: 연속 학습일(스트릭) 시스템 연결 — 마일스톤 XP 보너스 + 요약 탭 표시`
- 커밋 3: `fix: 저장 실패를 조용히 무시하던 8곳 추가 정리`
- GitHub main 브랜치 푸시: 커밋 1까지만 완료, 커밋 2·3은 로컬 커밋 상태 (푸시 대기)
- Edge Function 2개(`indices`, `economic-stats`) 배포 완료

---

## 2026-07-16

### 작업 내용

#### 배경 — Supabase 컬럼 누락으로 인한 연쇄 저장 실패

`profiles` 테이블에 실제로 존재하지 않는 컬럼에 계속 값을 저장하려 시도하고
있었음. 2026-06-22 (3차) 일지에 "SQL Editor에서 수동 실행 필요"로 적어둔
ALTER문이 실제로는 반영되지 않았던 것으로 보임. 총 3단계로 나눠 발견·수정.

#### AI 개인화 로드맵 연결 (`roadmap`, `noming_intro`)
- `completeOnboarding()`이 Solar AI로 생성한 개인화 로드맵을
  `profiles.roadmap`/`noming_intro`에 저장하려 했으나 컬럼이 없어 조용히
  실패 → `RoadmapPage.jsx`는 항상 정적 5단계 `roadmapData`만 표시하던 상태였음
- `RoadmapPage.jsx`: `userProfile.roadmap`(AI 결과, steps 3~5개 가변)이 있으면
  `normalizeAiRoadmap()`으로 변환해 렌더링, 없으면 기존 정적 로드맵으로 폴백
- `roadmapService.js`: `initProgress`가 고정 4단계 대신 `stepCount` 파라미터를
  받아 가변 스텝 수 처리 (기존엔 정적 5단계 로드맵인데도 4단계만 초기화하던
  버그도 같이 해결)
- `onboardingService.js`: 로드맵 생성 직후 실제 step 개수로 `initProgress` 재호출
- `ResultPage.jsx`: 진단 직후 초기화 시 정적 로드맵 길이(5) 명시적으로 전달
- 로드맵 단계별 "완료로 표시" 버튼 추가, `roadmap_progress` 테이블과 연동

#### 온보딩 필수 컬럼 긴급 수정 (`financial_goal`, `age_group`, `income_range`, `independence_score`)
- 이 4개는 `completeOnboarding()`의 **첫 번째** update(`if (error) throw error`로
  에러를 던지는 블록)에서 저장되는 값이라, 컬럼이 없으면 온보딩 자체가 에러를
  던지며 완료되지 못하는 더 심각한 케이스였음
- `OnboardingPage.jsx`의 `handleComplete`는 이 에러를 `console.error`로만
  남기고 사용자에게 아무 안내 없이 `/home`으로 조용히 리다이렉트 — 신규
  사용자가 이유도 모른 채 온보딩을 못 끝내고 있었을 가능성 (원인만 확인,
  이 UX 자체는 이번에 고치지 않음 — 다음 작업 후보로 아래에 기록)

#### 경제 자립 진단/로드맵/투두 기능 복구 (6개 컬럼)
- `independence_diagnosis`, `independence_roadmap`, `today_action`,
  `today_action_date`, `roadmap_completed_steps`, `roadmap_custom_todos`
  6개 컬럼 부재로 자립 진단 결과·AI 자립 로드맵·오늘의 행동·완료 스텝·
  커스텀 투두 저장이 전부 조용히 실패 (새로고침하면 사라지는 증상)
- `MyGrowthHubPage.jsx`의 `addCustomTodo`/`toggleCustomTodo`/`deleteCustomTodo`가
  `supabase.update()` 에러를 무시하고 있던 것도 같이 수정: 실패 시
  `console.error` 로그 + 낙관적 업데이트 롤백 + "저장에 실패했어요" 사용자 알림
- 자립 로드맵 `actions` 필드가 배열/객체(`{today, this_week, this_month}`)
  두 형식 모두 렌더링되도록 방어 코드 추가
- `generateIndependenceRoadmap` 프롬프트 재작성: 앱/사이트명과 구체적 수치를
  명시하도록 예시 강화, `actions`를 배열(3개 고정) → 객체 형식으로 변경,
  미사용 `relatedBites` 필드 제거

#### 노밍 코치 컨텍스트 기능
- `getCoachResponse`에 `context` 파라미터 추가 → 시스템 프롬프트에
  "현재 컨텍스트" 안내 삽입
- `CoachPage.jsx`: 다른 페이지에서 `location.state`로 넘어온
  `question`/`context`를 처리 후 state를 비워 새로고침 시 중복 전송 방지
- `MyGrowthHubPage.jsx`의 "노밍에게 이 단계 더 물어보기" 버튼과 연동

#### 버그 수정
- `OnboardingPage.jsx`: `FINANCIAL_GOALS`/`AGE_GROUPS`/`INCOME_RANGES`
  상수가 `STEPS` 배열보다 뒤에 선언되어 있어 TDZ(ReferenceError) 가능성 있었음
  → `STEPS`보다 앞으로 이동 (내용 변경 없이 순서만 수정)

### Supabase 스키마 변경 (오늘 총 12개 컬럼 추가 — `profiles`)
```sql
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS financial_goal          TEXT;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS age_group               TEXT;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS income_range            TEXT;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS independence_score      INTEGER;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS roadmap                 JSONB;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS noming_intro            TEXT;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS independence_diagnosis  JSONB;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS independence_roadmap    JSONB;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS today_action            TEXT;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS today_action_date       DATE;
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS roadmap_completed_steps INTEGER[] DEFAULT '{}';
ALTER TABLE profiles ADD COLUMN IF NOT EXISTS roadmap_custom_todos    JSONB DEFAULT '{}';
```
(Supabase 대시보드 SQL Editor에서 실행 완료 — `supabase/schema.sql`에도 동일하게 반영)

### 알려진 이슈 (다음 작업 후보)
- `OnboardingPage.jsx handleComplete`: 저장 실패 시 사용자에게 아무 안내 없이
  `/home`으로 조용히 리다이렉트됨. 컬럼 누락 문제는 해결됐지만, 향후 다른
  이유로 실패해도 사용자는 원인을 알 수 없음 — 최소한 에러 메시지 노출 필요
- `MyGrowthHubPage.jsx`의 `toggleStepComplete`(roadmap_completed_steps),
  `onboardingService.js`의 `generateTodayAction`(today_action 저장),
  `IndependenceDiagnosisPage.jsx`의 `handleSave`도 `supabase.update()` 에러를
  확인하지 않음 (오늘은 커스텀 투두 3개 함수만 수정) — 같은 패턴으로 정리 필요

### 커밋 (6개, main 브랜치)
```
19a8254 feat: 경제 자립 로드맵 프롬프트를 actions 객체 형식으로 개선
fd0853c fix: FINANCIAL_GOALS 등 상수 선언 순서로 인한 TDZ 에러 가능성 제거
cbd5c4d feat: 다른 페이지에서 넘어온 질문에 컨텍스트를 포함해 코치에게 전달하는 기능 추가
7984f7b fix: 경제 자립 진단/로드맵/커스텀 투두 저장이 컬럼 누락으로 조용히 실패하던 문제 해결
d7afc1c feat: AI 생성 개인화 로드맵을 RoadmapPage에 실제로 연결
449799a fix: 온보딩 완료 시 저장 실패를 유발하던 누락 컬럼 4개 추가
```

### 배포
- GitHub: `namong118/economing` main 브랜치 푸시 (커밋 6개)
- GitHub Pages 배포 완료

---

## 2026-06-22 (3차)

### 작업 내용

#### 경제 자립 기능 구현

**신규 파일: `IndependenceDiagnosisPage.jsx`** (라우트: `/independence`)
- 10문항 재무 자립도 진단 (비상금/저축/지출파악/부채/연금/보험/투자/절세/재무목표/금융이해)
- 각 문항 1~5점, 총 50점 만점
- 5단계 레벨: 씨앗(~18) / 새싹(~26) / 잎(~34) / 꽃(~42) / 열매(~50)
- 결과 화면: 전체 점수 + 카테고리별 점수 바 시각화
- 저장 시 `independence_score`, `independence_diagnosis` Supabase 저장
- 저장 후 `generateIndependenceRoadmap` + `generateTodayAction` 병렬 호출
- 완료 후 `/my-growth` (자립 탭) 이동

**`OnboardingPage.jsx`** — Step 5 추가
- Step 4: 재무 목표 선택 (FINANCIAL_GOALS 6가지: 내 집 마련/주식ETF/목돈/재무기초/노후준비/사업자금)
- Step 5: 나이대 선택 (AGE_GROUPS: 20대 초반 ~ 50대 이상)
- Step 6: 월 소득 구간 선택 (INCOME_RANGES: 소득없음 ~ 500만원 이상)
- 아이콘 없는 옵션 지원 (icon box 조건부 렌더링)
- `step.showDesc` 플래그로 6개 옵션에서도 설명 표시
- `Sunset` 아이콘 (노후 준비) import 추가
- CompletionScreen에 재무목표/나이대/월소득 항목 추가

**`onboardingService.js`**
- `completeOnboarding`에 `financial_goal`, `age_group`, `income_range` 저장 추가
- `generateIndependenceRoadmap(answers, independenceResult)` 추가
  - Solar AI로 4~6단계 자립 로드맵 JSON 생성
  - 투자 종목 추천/수익률 예측 절대 금지 시스템 프롬프트 포함
- `generateTodayAction(userId, financialGoal, independenceLevel)` 추가
  - 오늘 날짜로 캐시 확인 후 당일 행동 1가지 생성
  - Supabase `today_action`, `today_action_date` 업데이트

**`MyGrowthHubPage.jsx`** — 경제자립 탭 추가 (4탭 구조)
- 탭 레이아웃: 요약 / 경제자립 / 경제일기 / 경제사전 (4열 그리드)
- `IndependenceTab` 컴포넌트 신규:
  - 진단 전: "경제 자립 진단 시작하기 →" 버튼
  - 진단 후: 점수 원형 배지 + 단계 레이블 + 재진단 버튼
  - 로드맵: 단계별 목록 (현재 단계 초록 배지)
  - 오늘의 행동: 황색 테마 카드 + 일별 자동 갱신
- `navigate('/my-growth', { state: { tab: 'independence' } })` 로 자립 탭 직접 진입 지원
- `useLocation().state?.tab`으로 초기 탭 결정

**`HomePage.jsx`** — 오늘의 행동 제안 카드 추가
- 노밍 한마디 카드 아래에 황색 테마 카드 표시
- `profile?.today_action`이 있을 때만 렌더링
- "자립 로드맵 보기 →" 버튼으로 /my-growth 자립 탭 이동

**`App.jsx`**
- `/independence` 라우트 추가 (기존 `/diagnosis`는 경제 지식 진단 유지)

#### Supabase 스키마 변경 (SQL Editor에서 수동 실행 필요)
```sql
ALTER TABLE profiles
ADD COLUMN IF NOT EXISTS financial_goal TEXT,
ADD COLUMN IF NOT EXISTS age_group TEXT,
ADD COLUMN IF NOT EXISTS income_range TEXT,
ADD COLUMN IF NOT EXISTS independence_score INTEGER,
ADD COLUMN IF NOT EXISTS independence_diagnosis JSONB,
ADD COLUMN IF NOT EXISTS independence_roadmap JSONB,
ADD COLUMN IF NOT EXISTS today_action TEXT,
ADD COLUMN IF NOT EXISTS today_action_date DATE;
```

### 보안 원칙 유지
- 투자 종목 추천 절대 금지
- 수익률 예측 절대 금지
- Solar AI 프롬프트에 명시적 금지 조항 포함
- 금융 상품은 일반적 설명만 (ISA/IRP 개념 O, 가입 권유 X)

### 배포
- GitHub: `namong118/economing` main 브랜치 푸시
- GitHub Pages 배포 완료

---

## 2026-06-22 (2차)

### 작업 내용

#### 버그 수정

**추천 한잎 새로고침 시 변경 버그** (`biteService.js`)
- 모듈 레벨 `_recommendCache`는 페이지 새로고침 시 초기화 → Solar AI 재호출 → 다른 한잎 반환되는 문제
- `localStorage` 기반 캐시로 교체: 키 `economing_bite_rec__userId__userLevel__날짜`
- 같은 날 새로고침해도 동일한 추천 한잎 유지, 다음 날 자동으로 새 추천

**온보딩 페이지 스크롤·다음 이동 불가 버그** (`index.css`)
- 데스크톱(≥769px)에서 `.ds-app`이 `overflow: hidden`인데, 자식 요소에 height 제한 없어 `overflow-y: auto` 미동작
- `.ds-app > :not(.page)` 규칙을 `flex-shrink: 0; min-height: calc(100vh/1.15)` → `flex: 1; min-height: 0`으로 수정
- 로그인/회원가입/온보딩 등 PageWrapper 없는 페이지 전체 스크롤 복구

#### 노밍 한눈에 이해하기 — 이모지 → Lucide 아이콘 교체

**신규 파일: `InfographicIcon.jsx`**
- Lucide 아이콘 레지스트리 컴포넌트 생성 (`Building2`, `Wallet`, `PiggyBank`, `TrendingUp` 등 16종)
- 이후 AI 생성 인포그래픽에서도 재사용 가능하도록 `AVAILABLE_ICON_NAMES` 상수 export

**`infographicData.js`**
- 모든 icon 필드 이모지 문자열 → Lucide 아이콘 이름 문자열로 교체
- compare note 텍스트의 `💡` 이모지 제거

**`InfographicCard.jsx`, `FlowGraphic.jsx`, `CollectionGraphic.jsx`, `CompareGraphic.jsx`**
- `<span>{emoji}</span>` → `<InfographicIcon name={...} size={...} color={...} />` 교체
- FlowGraphic 마지막 스텝(완료): 아이콘 색상 `#fff`(흰색, 초록 배경 위)로 자동 분기

#### 랜딩페이지 앱 미리보기 목업 전면 개선 (`AppPreviewSection.jsx`)
- 기존 단순 placeholder → 실제 앱 코드 기반 정교한 목업으로 교체
- 탭 4개: 홈 / 노밍 / 경제한잎 / 경제일기
- **홈**: 그린 그라디언트 XP 카드(닉네임·레벨배지·XP바·할일 pills) + 다크 포레스트 한잎 카드 + 노밍 한마디 섹션
- **노밍**: 노밍 인사 카드(Sun 아이콘) + 추천 질문 목록(ChevronRight) + 실제 대화 버블 + 입력창
- **경제한잎**: 다크 히어로 배너(Leaf 아이콘) + 카테고리 필터 칩 + BiteCard 스타일 목록
- **경제일기**: 날짜 헤더 + 섹션 카드(BookOpen·Newspaper·Lightbulb 아이콘) + 지난 일기 목록

#### 노밍 한눈에 이해하기 — AI 동적 생성 폴백 추가

**신규 파일: `infographicService.js`**
- 동작 흐름:
  1. 정적 데이터 매칭 → 있으면 즉시 반환
  2. 경제 개념 키워드 감지 → 있으면 Solar AI 생성
  3. 키워드 없으면(대화성 질문) → null (인포그래픽 없음)
- Solar AI에 `flow/collection/compare` 3가지 스키마와 사용 가능 아이콘 목록 전달
- 응답 유효성 검사 후 모듈 레벨 캐시 저장
- 경제 개념 키워드 30+개 정의 (주식, 채권, 복리, 환율, 전세, IRP, 연말정산 등)

**`InfographicIcon.jsx` 아이콘 확장**
- 16종 → 33종으로 확장: ArrowUp/Down, Home, Briefcase, Receipt, Shield, Calendar, Clock, Users, Percent, RefreshCw, DollarSign, Award, LineChart, PieChart, Car, Scale, Coins 추가

**`CoachPage.jsx`**
- `getInfographic(q)` → `getOrGenerateInfographic(q)` 교체
- 노밍 응답(`getCoachResponse`)과 인포그래픽 생성을 `Promise.all`로 병렬 실행 → 레이턴시 최소화

| 질문 예시 | 결과 |
|---|---|
| "ETF가 뭔가요?" | 정적 데이터 즉시 반환 |
| "복리가 뭔가요?" | Solar AI 생성 |
| "전세 vs 월세 차이" | Solar AI 생성 |
| "요즘 경제 어때요?" | null (인포그래픽 없음) |

### 배포
- GitHub: `namong118/economing` main 브랜치 푸시 (커밋 4개)
- GitHub Pages 배포 완료

---

## 2026-06-22 (1차)

### 작업 내용

#### 버그 수정

**추천 한잎 탭 이동 시 변경 버그** (`biteService.js`)
- 홈 ↔ 경제한잎 탭 이동 시 `getRecommendedBite`가 매 마운트마다 Solar AI를 재호출해 추천 bite가 바뀌던 문제
- 모듈 레벨 `_recommendCache` 추가: `userId__userLevel__날짜` 키로 캐시 → 하루 한 번만 AI 호출, 이후 즉시 반환

**홈 화면 할일 체크 Supabase 404 오류** (`HomePage.jsx`)
- `Promise.all` → `Promise.allSettled` 교체: 하나의 쿼리가 실패해도 나머지 할일 체크가 독립적으로 동작
- `diary` → `economic_journals` 테이블명 수정 (잘못된 테이블 참조 버그)
- 원인: 실제 경제일기는 `economic_journals` 테이블을 사용하는데 홈 체크 쿼리만 `diary`로 잘못 기재됨

**개발 서버 404 오류** (`vite.config.js`)
- `base: command === 'build' ? '/economing/' : '/'` → `base: '/economing/'` 고정
- 개발 서버(`localhost:5173`)에서 `/economing/` 경로로 접근 시 리소스를 찾지 못하던 문제 해결

#### 퀴즈 결과 저장 기능 추가 (`EconomicBitePage.jsx`)
- 퀴즈 정답 시 `user_quiz_results` 테이블에 `{ user_id, bite_id, is_correct: true }` 저장
- `supabase` 클라이언트 import 추가
- Supabase `user_quiz_results` 테이블 신규 생성 (RLS 포함):
  ```
  id, user_id, bite_id, is_correct, created_at
  ```
- 홈 화면 "한잎 퀴즈 풀기" pill이 오늘 정답을 맞춘 경우 완료 체크로 표시됨

#### 온보딩 결과 화면 정리 (`OnboardingPage.jsx`)
- AI 결과 화면(`AIResultScreen`)에서 학습 로드맵 섹션 전체 제거
- "로드맵 자세히 보기 →" 버튼 제거
- 이모지 전체 제거 (헤더 🎉, 노밍 인트로 fallback 🌱, CompletionScreen 제목 🎉)
- 부제목 문구 변경: "당신만을 위한 경제 성장 로드맵이에요" → "이제 당신만의 경제 성장 여정을 시작해볼게요"

### Supabase 스키마 변경
- `user_quiz_results` 테이블 신규 생성
- RLS 정책: 본인 데이터만 INSERT/SELECT 가능

### 배포
- GitHub: `namong118/economing` main 브랜치 푸시
- GitHub Pages 배포 완료

---

## 2026-06-19

### 작업 내용

#### 진단 페이지 전면 재설계
- 기존 5문항/2지선다 → **10문항/5단계 점수 선택지(1~5점)** 구조로 변경
- 총점 10~50점 범위, 5개 레벨 분류 (beginner / elementary / intermediate / advanced / expert)
- 카테고리별 아이콘 표시, 점수 뱃지, 진행 바 포함
- 컨텐츠 상단 배치 (`padding-bottom: 160px`)

#### 결과 페이지 재설계
- 로드맵 섹션 전체 제거 → 결과 카드만 간결하게 표시
- 카드 크기 소형화 (maxWidth 440px, 아이콘 56px, 제목 26px)
- "학습하러 가기" 버튼: 비로그인 → `/home`, 로그인 → 온보딩
- 점수 바 음수 방지 (`Math.max(0, ...)`)
- `roadmapData`에 `advanced` / `expert` 항목 추가 (undefined → map() 크래시 수정)

#### TopNav
- 로고 가운데 고정 (`position: absolute; left: 50%; transform: translateX(-50%)`)
- 로고 사이즈 68px
- 햄버거 메뉴 및 모바일 드롭다운 전체 제거

#### DesktopShell (좌측 브랜드 패널)
- 텍스트 로고(`econ●ming`) → **앱아이콘 이미지** (160×160, rounded 36)로 교체
- QR 이미지 → 📱 목업 플레이스홀더 ("곧 제공 예정") 으로 대체
- 패널 내부 전체 가운데 정렬

#### CoachPage (노밍 탭)
- 2컬럼 사이드바 레이아웃 완전 제거 → 단일 세로 컬럼
- 기록 드로어: viewport fixed → `coach-page-wrap` 내부 `position: absolute` 좌측 슬라이드 패널
- "기록" 버튼 NomingGreeting 카드 안으로 이동
- 인삿말 이모지 제거

#### HomePage
- 비로그인 방문자용 상단 카드 추가 (그라디언트 스타일, 절반 크기)
- 오늘의 할일 항목: 한잎 읽기 / 한잎 퀴즈 풀기 / 노밍과 대화하기 / 경제일기 쓰기

#### EconomicBiteArchivePage
- 상단 "오늘의 경제 한잎" 배너 → 다크 히어로 카드 스타일로 교체

#### LoginPage / SignupPage
- 앱 아이콘 + 로고 이미지 제거
- 컨텐츠 위쪽 배치 (`padding: 12px 20px 120px`)

#### 기타
- `vite.config.js`: `server.watch.ignored: ['**/AppData/**']` 추가 (Parsec lock 파일 EBUSY 오류 방지)

### 배포
- GitHub: `namong118/economing` main 브랜치 푸시
- GitHub Pages 배포 완료

---

## 이전 작업 요약 (컨텍스트 압축 이전)

| 날짜 | 주요 작업 |
|------|-----------|
| ~ 2026-06-10 | React+Vite 프로젝트 초기 세팅, Supabase 연동, 라우팅, XP/레벨 로직 |
| ~ 2026-06-10 | 랜딩페이지 전체 섹션 구성 (Hero, Problem, Value, Feature, GrowthFlow, AppPreview, ComingSoon, FinalCTA) |
| ~ 2026-06-10 | 홈, 노밍, 경제한잎, 내성장, 경제읽기, 경제일기 페이지 기본 구현 |
| ~ 2026-06-10 | DesktopShell 2단 레이아웃 (좌측 브랜드 패널 + 우측 480px 앱 컬럼) |
| ~ 2026-06-10 | XP 시스템, 퀴즈 정답 시 XP 적립, 읽기 완료 시 XP 적립 |
